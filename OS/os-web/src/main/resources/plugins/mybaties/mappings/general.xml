<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.teamax.gisEngine.core.idao.IWmtsMetaDataDao">
    <insert id="generalInsert" parameterType="org.lemonPig.os.assist.mybaties.bean.Crud.Insert">
        <selectKey keyProperty="id" resultType="Long" order="BEFORE">
    		SELECT GN_WMTS_METADATA_PK_SEQ.NEXTVAL FROM dual
    	</selectKey>
    	INSERT INTO GN_WMTS_METADATA (
   			<foreach collection="list" item="item" index="index" separator="UNION ALL">
       			WTMETA_ID,WTMETA_CRS,WTMETA_MINX,WTMETA_MINY,WTMETA_MAXX,WTMETA_MAXY,OGC_ID
   			</foreach>
       	) VALUES (
        	#{id},#{crs},#{minX},#{minY},#{maxX},#{maxY},#{ogc.id}        
        )
    </insert>
</mapper>
